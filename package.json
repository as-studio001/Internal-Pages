{
  "name": "internal-pages-backend",
  "private": true,
  "description": "Netlify Functions backend for the Internal-Pages admin: manages who can log in (via Supabase email/password + invite) and proxies the actual GitHub writes using one shared repo token, so individual admin users never need their own GitHub account.",
  "dependencies": {
    "@supabase/supabase-js": "^2.45.4"
  }
}
